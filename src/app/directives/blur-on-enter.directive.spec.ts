import { BlurOnEnterDirective } from './blur-on-enter.directive';

describe('BlurOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
